Remaining renames needed in App.tsx:

1. addCollection → addProject
2. deleteCollection → deleteProject
3. startEditingCollection → startEditingProject  
4. saveCollectionName → saveProjectName
5. cancelEditingCollection → cancelEditingProject
6. editingCollectionId → editingProjectId
7. editingCollectionName → editingProjectName
8. setEditingCollectionId → setEditingProjectId
9. setEditingCollectionName → setEditingProjectName
10. exportCollectionJSON → exportProjectJSON (already done)
11. importCollectionJSON → importProjectJSON (already done)
12. activeCollectionId → activeProjectId (many references in JSX)
13. setActiveCollectionId → setActiveProjectId (many references in JSX)
14. collections → projects (all references in JSX map functions, etc.)
15. collection → project (in JSX loops)
16. "Collection" strings → "Project" strings in UI
17. TokenCollection type → TokenProject type
18. collectionId properties → projectId properties
